/**
 * Library listing lives in SourcesManager when pathname is `/sources`.
 * This page is intentionally empty — see `app/(app)/(workspace)/layout.tsx`.
 */
export default function SourcesPage() {
  return null;
}
