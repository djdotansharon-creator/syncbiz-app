import { MobileJingles } from "@/components/mobile/mobile-jingles";

/**
 * /mobile/jingles — M1: create a jingle from text (Generate → local Preview → Regenerate → Save).
 * Renders inside the mobile shell (header + bottom nav). OFF-PLAYBACK: no PLAY/PLAY_INTERRUPT.
 */
export default function MobileJinglesPage() {
  return <MobileJingles />;
}
