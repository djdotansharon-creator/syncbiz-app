/**
 * MPV bridge — typed surface between playback agent and MPV IPC (design placeholder).
 * No JSON IPC implementation in Phase 1 skeleton.
 * @see docs/MPV-INTEGRATION-PLAN-v1.md
 */

/** Placeholder until the bridge implements real request/response typing. */
export type MpvBridgeNotImplemented = {
  readonly _brand: "mpv-bridge-not-implemented";
};
