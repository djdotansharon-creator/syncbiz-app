/** Injected by `scripts/bundle-desktop.cjs` (esbuild `define`). */
declare const __DESKTOP_DEV_DEBUG_PANEL__: boolean;
/** When false, JINGLES CONTROL operator rail + drawer are not mounted (no layout hooks). */
declare const __DESKTOP_JINGLES_CONTROL_UI__: boolean;
