/**
 * PlaylistPro path helpers — shared between main and renderer.
 * Stub: this build does not include PlaylistPro library detection.
 */

/**
 * Returns a friendly display label if the given path is a PlaylistPro library root,
 * or null if it is a regular user-chosen folder.
 * Stub returns null (no PlaylistPro detection in this build).
 */
export function musicFolderDisplayLabel(_path: string | null | undefined): string | null {
  return null;
}
