<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Optional "Start on boot" (best-effort; see BootReceiver / README). -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Start-on-boot uses a full-screen-intent notification to surface the player from
         the background (bare startActivity from BOOT_COMPLETED is blocked on Android 12+/14). -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Native appliance foreground service. Phase 1 = specialUse (persistent connectivity,
         NO audio). mediaPlayback is added only in Phase 2 when the service owns playback. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Generic Android TV / Google TV. leanback NOT required so the same APK also
         installs on Android TV boxes/sticks and non-TV Android as a fallback. -->
    <uses-feature android:name="android.software.leanback" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:banner="@drawable/banner"
        android:label="@string/app_name"
        android:theme="@style/Theme.VonoStreamer"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|smallestScreenSize|uiMode|navigation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!-- Android TV / Google TV home row -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
                <!-- Also list in a normal launcher (boxes/sticks without a leanback UI) -->
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Native settings screen (opened via MENU / long-press BACK from the player). -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="@string/settings_title"
            android:theme="@style/Theme.VonoStreamer" />

        <!-- Native appliance core service (Phase 1 shadow). Runs outside the Activity.
             Phase 1 type = specialUse (persistent cloud/WS connectivity, no audio yet).
             Phase 2 switches to mediaPlayback when the service actually owns playback. -->
        <service
            android:name=".VonoStreamerService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Maintains the persistent VONO Cloud control/WebSocket connection for an unattended branch streamer appliance (device presence, MASTER/CONTROL state, remote-command channel). No audio in this phase." />
        </service>

        <!-- Optional start-on-boot (best-effort; may be restricted on Android 14 / some OEMs). -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
